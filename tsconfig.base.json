{
  "description": "The base configuration for the project. Extended configs are responsible for including files and producing outputs.",
  "extends": "@tsconfig/node20",
  "compilerOptions": {
    "noEmit": true
  }
}
